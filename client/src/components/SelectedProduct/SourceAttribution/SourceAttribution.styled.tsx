import styled from 'styled-components';

export const SourceAttribution = styled.a`
  font-size: 0.7em;
  display: block;
  margin-top: 0.5em;
  margin-bottom: 1.5em;

  &:visited,
  &:link {
    color: rgb(190, 190, 190);
  }
`;
