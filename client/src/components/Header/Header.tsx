import styled from 'styled-components';

const Header = () => {
  return <StyledHeader></StyledHeader>;
};

export default Header;

const StyledHeader = styled.header`
  height: 6em;
  background-color: rgba(134, 178, 232, 0.2);
`;
